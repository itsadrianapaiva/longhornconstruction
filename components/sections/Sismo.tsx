import { SectionShell } from "./SectionShell";

export default async function Sismo() {
  return (
    <SectionShell
      id="sismo"
      title="Sismo Method"
      subtitle="Faster shell, stronger structure, cleaner site."
    >
      <div className="rounded-xl border border-zinc-200 p-6 text-sm text-zinc-700">
        Placeholder for a short Sismo time-lapse clip.
      </div>
    </SectionShell>
  );
}
